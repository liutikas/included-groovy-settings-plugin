apply from: "../out-setup.groovy"

getGradle().beforeProject { project ->
    def checkoutRoot = new File("${buildscript.sourceFile.parent}/..")
    init.chooseBuildDirectory(checkoutRoot, rootProject.name, project)
}